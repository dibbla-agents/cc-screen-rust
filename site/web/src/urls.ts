// The [0054] URL contract — every external target the page links to, in one
// place, so a staging build can override it and the funnel can't drift.
export const APP = "https://app.ccscreen.dev"; // hosted hub (proposal 0053)
export const SITE = "https://ccscreen.dev"; // this page (GitHub Pages custom domain)
export const DOCS = "./docs/"; // 0055's docs tree (relative — works on every host)
export const GITHUB = "https://github.com/dibbla-agents/cc-screen-rust";
